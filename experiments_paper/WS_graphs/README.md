The file `WS_data.npz` contains the average distance between a WS graph (n = 1000, m = 10,000) with p = 0 and WS graphs with p > 0. This data can be reproduced by running the script `WS_distance_to_p0.py`.  
  
Fig. 2 in the manuscript can be reproduced by running `plot.py`.
